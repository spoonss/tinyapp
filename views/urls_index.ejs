<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <header>
        <% include ./partials/_header.ejs %>
    </header>
    <h1>List of URLs</h1>
    <ul>
        
        <% for (let key in urls) { %>
            <% if (urls[key].userID === loggedIn){ %>
            <li>Short URL: <a href ="<%= key %>"><%= key %></a> ---> <a href ="<%= urls[key].longURL%>"><%= urls[key].longURL%></a>
                <span></span><form action="/urls/<%= key %>">
                    <input type="submit" value="Edit" />
                </form></span>
                <span><form action="/urls/<%= key %>/delete" method="POST" >
                    <input type="submit" value="Delete">
                </form></span>
               
            </li>
            <% } %>
        <% } %>
    </ul>

    <h4><a href="/urls/new">Link to add new URL</a></h4>

</body>
</html>


